# X (Twitter) API credentials
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=

# OpenAI
OPENAI_API_KEY=

# Upstash Redis (for persistent state on Railway)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# AWS (for DynamoDB state store — only if USE_DYNAMODB=true)
# USE_DYNAMODB=false
# AWS_REGION=us-east-1
# DYNAMODB_TABLE_NAME=nonojinx-state

# Bot config
DRY_RUN=true
MIN_INNING_THRESHOLD=1
POLL_INTERVAL_MS=60000
